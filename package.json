{
  "name": "blockchain",
  "version": "1.0.0",
  "dependencies": {
    "get-json": "^1.0.1",
    "lite-server": "^2.4.0"
  },
  "scripts": {
    "dev": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
